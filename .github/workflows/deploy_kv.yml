name: Deploy package "KV" to NPM

on:
  workflow_dispatch:
  push:
    tags:
      - 'kv-[0-9].[0-9].[0-9]'
      
permissions:
  id-token: write  # Required for OIDC
  contents: read

jobs:
  deploy_kv:
    uses: ./.github/workflows/_deploy.yml
    with:
      package-name: kv
    secrets: inherit